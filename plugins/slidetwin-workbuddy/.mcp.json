{
  "mcpServers": {
    "slidetwin-tools": {
      "command": "node",
      "args": [
        "${CODEBUDDY_PLUGIN_ROOT}/scripts/launch-mcp.mjs"
      ],
      "cwd": "${CODEBUDDY_PLUGIN_ROOT}"
    }
  }
}
